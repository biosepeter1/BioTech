<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogSpark - Your Dashboard</title>
    
    <!-- Preload resources -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/animate.css@4.1.1/animate.min.css"/>
    
    <!-- Tailwind CSS and SweetAlert2 via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    <style>
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .animate-fadeIn { animation: fadeIn 0.6s ease-out forwards; }
        .animate-slideUp { animation: slideUp 0.6s ease-out forwards; }
        
        .frosted-glass {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(12px);
        }
        
        .post-card:hover .post-title {
            color: #3b82f6;
        }
        
        .category-badge {
            transition: all 0.3s ease;
        }
        
        .category-badge:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>

<body class="font-['Inter'] bg-gray-50 dark:bg-gray-900 min-h-screen transition-colors duration-300">
    <!-- Header -->
    <%- include('partials/header copy 2') %>

    <!-- Welcome Section -->
    <section class="mt-16 text-center animate-slideUp">
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-800 dark:text-gray-100 mb-4">
            Welcome Back!
        </h2>
        <p class="text-gray-600 dark:text-gray-300 mb-6 max-w-lg mx-auto">
            Ready to ignite your ideas? Create a new post or explore your stories below.
        </p>
        <a href="/posts" class="inline-block bg-cyan-500 hover:bg-cyan-600 text-white font-semibold py-3 px-8 rounded-full shadow-lg transition-all duration-300">
            Create New Post
        </a>
    </section>

    <!-- Main Content -->
    <main class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <!-- Featured Post -->
        <% if (posts.length > 0) { %>
            <section class="mb-16 animate-fadeIn">
                <h2 class="text-2xl sm:text-3xl font-semibold text-gray-800 dark:text-gray-100 mb-6 text-center">
                    Your Story
                </h2>
                <article class="frosted-glass rounded-xl shadow-lg overflow-hidden transition-all duration-300 hover:shadow-xl">
                    <div class="md:flex">
                        <div class="md:w-1/3">
                            <img src="<%= posts[0].image || '/images/placeholder.jpg' %>" alt="<%= posts[0].title %>" class="w-full h-64 md:h-full object-cover">
                        </div>
                        <div class="p-6 md:w-2/3">
                            <div class="flex items-center mb-3">
                                <span class="category-badge bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 text-xs font-medium px-3 py-1 rounded-full">
                                    <%= posts[0].category %>
                                </span>
                                <span class="text-gray-500 dark:text-gray-400 text-sm ml-3">
                                    <%= new Date(posts[0].date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                                </span>
                            </div>
                            <h3 class="post-title text-xl sm:text-2xl font-bold text-gray-800 dark:text-gray-100 mb-2">
                                <%= posts[0].title %>
                            </h3>
                            <p class="text-gray-600 dark:text-gray-300 mb-4">
                                <%= posts[0].content.substring(0, 180) %>...
                            </p>
                            <div class="flex items-center justify-between">
                                <span class="text-sm text-gray-500 dark:text-gray-400">
                                    By <%= posts[0].author || 'Anonymous' %>
                                </span>
                                <div class="flex space-x-4">
                                    <a href="/posts/<%= posts[0].id %>" class="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 text-md font-medium">
                                        View
                                    </a>
                                    <a href="/posts/<%= posts[0].id %>/edit" class="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 text-md font-medium">
                                        Edit
                                    </a>
                                    <form action="/posts/<%= posts[0].id %>?_method=DELETE" method="POST">
                                        <button type="button" onclick="confirmDelete('<%= posts[0].id %>')" class="text-red-600 hover:text-red-800 dark:text-red-400 dark:hover:text-red-300 text-md font-medium">
                                            Delete
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </article>
            </section>
        <% } %>

        <!-- Latest Posts -->
        <section class="animate-fadeIn">
            <h2 class="text-2xl sm:text-3xl font-semibold text-gray-800 dark:text-gray-100 mb-6 text-center">
                Latest Stories
            </h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <% posts.forEach(post => { %>
                    <article class="post-card bg-white dark:bg-gray-800 rounded-lg shadow-sm overflow-hidden transition-all duration-300 hover:shadow-md">
                        <img src="<%= post.image || '/images/placeholder.jpg' %>" alt="<%= post.title %>" class="w-full h-48 object-cover rounded-t-lg">
                        <div class="p-5">
                            <div class="flex items-center mb-3">
                                <span class="category-badge bg-<%= post.category === 'Programming' ? 'blue' : post.category === 'Web Design' || post.category === 'Technology' ? 'green' : 'gray' %>-100 dark:bg-<%= post.category === 'Programming' ? 'blue' : post.category === 'Web Design' || post.category === 'Technology' ? 'green' : 'gray' %>-900 text-<%= post.category === 'Programming' ? 'blue' : post.category === 'Web Design' || post.category === 'Technology' ? 'green' : 'gray' %>-700 dark:text-<%= post.category === 'Programming' ? 'blue' : post.category === 'Web Design' || post.category === 'Technology' ? 'green' : 'gray' %>-300 text-xs font-medium px-3 py-1 rounded-full">
                                    <%= post.category %>
                                </span>
                                <span class="text-gray-500 dark:text-gray-400 text-sm ml-3">
                                    <%= new Date(post.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %>
                                </span>
                            </div>
                            <h3 class="post-title text-lg font-semibold text-gray-800 dark:text-gray-100 mb-2">
                                <%= post.title %>
                            </h3>
                            <p class="text-gray-600 dark:text-gray-300 text-sm mb-4">
                                <%= post.content.substring(0, 100) %>...
                            </p>
                            <div class="flex items-center justify-between">
                                <span class="text-sm text-gray-500 dark:text-gray-400">
                                    By <%= post.author || 'Anonymous' %>
                                </span>
                                <div class="flex space-x-4">
                                    <a href="/posts/<%= post.id %>" class="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 text-md font-medium">
                                        View
                                    </a>
                                    <a href="/posts/<%= post.id %>/edit" class="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 text-md font-medium">
                                        Edit
                                    </a>
                                    <form id="delete-form-<%= post.id %>" action="/posts/<%= post.id %>?_method=DELETE" method="POST">
                                        <button type="button" onclick="confirmDelete('<%= post.id %>')" class="text-red-600 hover:text-red-800 dark:text-red-400 dark:hover:text-red-300 text-md font-medium">
                                            Delete
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </article>
                <% }); %>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <div><%- include('partials/footer.ejs') %></div>
    <!-- Scripts -->
    <script src="/scripts.js"></script>
</body>
</html>